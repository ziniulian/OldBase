
参数：
	obj：Bean对象（upd 时，以数组形式，首元素为改变之内容，次元素为查询之条件）
	opt：{
		back：回调信息，
		method：操作方法（sav、del、qry、ins、upd），
		class：类型（当obj非 BeanJson形式时，必须由此参数来指定类型。且不支持数组形式，故基本无用）
	}

返回值：
	{
		statue：服务状态（0：成功，非零：失败），
		memo：备注，
		obj：结果（BeanJson形式），
		back：回调信息
	}

